# Moved: canonical Lean sources now live under `lean/`

The full contents of this file were moved into `lean/` in branch tidy/move-lean-sources.
To avoid duplicate large copies, this root-level file was replaced with this pointer.

If you need the original filename at the repository root for tooling reasons, tell me and I can revert or adjust.
